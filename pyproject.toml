[project]
name = "job-scout-agent"
version = "0.1.0"
description = "AI agent for job market analysis"
requires-python = ">=3.11"
dependencies = [
    "httpx>=0.27",
    "selectolax>=0.3",
    "python-dotenv>=1.0",
]
